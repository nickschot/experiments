<div local-class="root" ...attributes {{did-insert this.render}} {{did-resize this.render}}>
  <div local-class="content" data-test-svg-box-content>
    {{yield}}
  </div>
  <svg local-class="svg" width={{this.width}} height={{this.height}} viewBox={{this.viewBox}}>
    <path
      local-class="bg"
      d="{{this.path}}"
      fill="none"
      data-test-svg-box-bg
    />

    {{#if @simpleAnimation}}
      <path
        local-class="border"
        d="{{this.path}}"
        stroke-width="{{this.strokeWidth}}"
        stroke-dasharray="{{this.circumference}}"
        stroke-dashoffset="{{this.circumference}}"
        stroke="#88929c"
        fill="none"
        data-test-svg-box-border-simple
      />
    {{else}}
      <path
        local-class="border"
        d="{{this.pathTop}}"
        stroke-width="{{this.strokeWidth}}"
        stroke-dasharray="{{this.halfCircumference}}"
        stroke-dashoffset="{{this.halfCircumference}}"
        stroke="#88929c"
        fill="none"
        data-test-svg-box-border-top
      />
      <path
        local-class="border"
        d="{{this.pathBottom}}"
        stroke-width="{{this.strokeWidth}}"
        stroke-dasharray="{{this.halfCircumference}}"
        stroke-dashoffset="{{this.halfCircumference}}"
        stroke="#88929c"
        fill="none"
        data-test-svg-box-border-bottom
      />
    {{/if}}
  </svg>
</div>